# Core LangChain stack (compatible with initialize_agent)
langchain==0.1.16
langchain-core==0.1.43
langchain-community==0.0.33
langchain-groq==0.0.6

# Streamlit for UI
streamlit==1.37.0

# Env management
python-dotenv==1.0.1

# Groq client dependency (auto-installed with langchain-groq, but just in case)
groq==0.3.0

# Web search + info tools
duckduckgo-search==5.3.0
wikipedia==1.4.0
arxiv==2.1.0

# Optional safety + helper packages
pydantic==2.7.1
requests==2.32.3
